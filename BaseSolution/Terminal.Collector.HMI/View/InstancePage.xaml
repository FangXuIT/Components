<Page x:Class="Terminal.Collector.HMI.View.InstancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Terminal.Collector.HMI.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InstancePage">

    <Grid>
        <DataGrid x:Name="gdIntance" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="PLC名称" Width="100" Binding="{ Binding Path=Name}" />
                <DataGridTextColumn Header="PLC类型" Width="100" Binding="{ Binding Path=CpuType}" />
                <DataGridTextColumn Header="IP" Width="100" Binding="{ Binding Path=IP}" />
                <DataGridTextColumn Header="Port" Width="100" Binding="{ Binding Path=Port}" />
                <DataGridTextColumn Header="Slot" Width="100" Binding="{ Binding Path=Slot}" />
                <DataGridTextColumn Header="Rack" Width="100" Binding="{ Binding Path=Rack}" />
                <DataGridCheckBoxColumn Header="连接状态" Width="100" Binding="{ Binding Path=IsConnected}" />
                <DataGridTextColumn Header="备注信息" Width="100" Binding="{ Binding Path=Message}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
